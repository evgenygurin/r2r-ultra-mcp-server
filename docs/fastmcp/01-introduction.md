# Введение в FastMCP

## Что такое FastMCP?

FastMCP — это быстрый, Pythonic фреймворк для создания серверов и клиентов Model Context Protocol (MCP). Он упрощает создание приложений, интегрированных с LLM, предоставляя простые инструменты для развертывания, аутентификации и динамической генерации инструментов.

## Основные возможности

### 🚀 Быстрый старт
- Минималистичный API для создания MCP серверов
- Автоматическая генерация схем из type hints
- Поддержка синхронных и асинхронных функций

### 🔐 Корпоративная аутентификация
- Нулевая конфигурация для провайдеров (Google, GitHub, WorkOS, Azure, Auth0)
- JWT токен верификация
- OAuth 2.0 интеграция

### 🌐 Гибкое развертывание
- Поддержка stdio и HTTP транспортов
- FastMCP Cloud для быстрого развертывания
- Интеграция с FastAPI и OpenAPI

### 🛠️ Мощные компоненты
- **Tools**: Функции, которые LLM могут вызывать для выполнения действий
- **Resources**: Данные, которые LLM могут читать
- **Prompts**: Переиспользуемые шаблоны сообщений

## Быстрый старт

### Установка

```bash
pip install fastmcp
```

### Создание простого сервера

```python
from fastmcp import FastMCP

# Создаем сервер
mcp = FastMCP("My MCP Server")

# Добавляем инструмент
@mcp.tool
def greet(name: str) -> str:
    """Приветствует пользователя по имени."""
    return f"Привет, {name}!"

# Запускаем сервер
if __name__ == "__main__":
    mcp.run()
```

### Запуск сервера

Существует два способа запустить сервер:

#### 1. Через Python скрипт
```bash
python my_server.py
```

#### 2. Через FastMCP CLI
```bash
fastmcp run my_server.py
```

**Примечание**: FastMCP CLI **не** выполняет блок `__main__` вашего файла. Вместо этого он импортирует объект сервера и запускает его с указанным транспортом.

### Выбор транспорта

#### Stdio (локальное использование)
```python
if __name__ == "__main__":
    mcp.run(transport="stdio")
```

#### HTTP (удаленный доступ)
```python
if __name__ == "__main__":
    mcp.run(transport="http")
```

## Подключение к серверу

### Использование FastMCP Client

```python
import asyncio
from fastmcp import Client

client = Client("http://localhost:8000/mcp")

async def call_tool(name: str):
    async with client:
        result = await client.call_tool("greet", {"name": name})
        print(result)

asyncio.run(call_tool("Ford"))
```

### Ключевые моменты использования клиента
- FastMCP клиенты асинхронны, используйте `asyncio.run`
- Обязательно используйте контекстный менеджер `async with client:`
- Можно выполнять несколько вызовов в одном контексте

## Развертывание в FastMCP Cloud

FastMCP Cloud — это бесплатный хостинг для персональных серверов с простым pay-as-you-go для команд.

### Преимущества
- Оптимизирован для развертывания аутентифицированных FastMCP серверов
- Безопасный URL для подключения из любого LLM клиента
- Автоматическое определение зависимостей из `requirements.txt` или `pyproject.toml`

### Шаги развертывания

1. Посетите [fastmcp.cloud](https://fastmcp.cloud)
2. Войдите через GitHub
3. Создайте проект из своего репозитория
4. Настройте конфигурацию:
   - **Name**: Имя проекта (будет использоваться в URL)
   - **Entrypoint**: Python файл с сервером (например, `echo.py`)
   - **Authentication**: Включить/выключить публичный доступ

5. После развертывания ваш сервер будет доступен по адресу:
   ```
   https://your-project-name.fastmcp.app/mcp
   ```

## Архитектура FastMCP

```
┌─────────────────────────────────────────┐
│         FastMCP Server                  │
├─────────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐  ┌────────┐│
│  │  Tools   │  │Resources │  │Prompts ││
│  └──────────┘  └──────────┘  └────────┘│
├─────────────────────────────────────────┤
│         Middleware Layer                │
│  (Auth, Logging, Error Handling)        │
├─────────────────────────────────────────┤
│        Transport Layer                  │
│    (stdio / HTTP / SSE)                 │
└─────────────────────────────────────────┘
           ↕
    ┌─────────────┐
    │   Clients   │
    │  (LLM Apps) │
    └─────────────┘
```

## Следующие шаги

- [Tools](./02-tools.md) - Создание инструментов для LLM
- [Resources](./03-resources-prompts.md) - Работа с ресурсами и промптами
- [Client](./04-client-connection.md) - Подключение к серверам
- [Authentication](./05-authentication.md) - Настройка безопасности
- [Deployment](./06-deployment-configuration.md) - Развертывание в продакшн
