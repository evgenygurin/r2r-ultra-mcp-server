# Развертывание на FastMCP Cloud ☁️

**FastMCP Cloud** - это платформа для хостинга MCP серверов от команды FastMCP (Prefect).  
Бесплатна для личного использования! 🎉

## 📋 Предварительные требования

1. **GitHub аккаунт**: https://github.com
2. **Репозиторий на GitHub** с вашим FastMCP сервером
3. **Файлы в репозитории**:
   - Python файл с сервером (например, `server.py`)
   - `pyproject.toml` или `requirements.txt` (для зависимостей)
   - `.env.example` (опционально, для документирования переменных окружения)

## 🚀 Пошаговая инструкция

### Шаг 1: Подготовка репозитория

#### 1.1. Проверьте код на GitHub

Убедитесь, что ваш сервер загружен на GitHub:

```bash
cd /Users/laptop/dev/r2r-fastmcp/mcp-server

# Проверьте статус
git status

# Закоммитьте изменения (если есть)
git add server.py pyproject.toml README.md
git commit -m "Prepare server.py for FastMCP Cloud deployment"

# Запушьте на GitHub
git push origin main
```

#### 1.2. Проверьте совместимость

Убедитесь, что ваш сервер работает локально:

```bash
# Проверка синтаксиса
python -m py_compile server.py

# Инспекция сервера (покажет что увидит FastMCP Cloud)
fastmcp inspect server.py:mcp
```

**Важно**: Убедитесь, что в `server.py` есть объект `mcp`:

```python
# server.py
from fastmcp import FastMCP

mcp = FastMCP("R2R MCP Server Ultra")  # ← Этот объект

# ... ваши инструменты, middleware, и т.д.
```

### Шаг 2: Настройка переменных окружения

FastMCP Cloud поддерживает переменные окружения через веб-интерфейс.

#### 2.1. Создайте `.env.example` для документации

```bash
# .env.example
R2R_BASE_URL=https://your-r2r-instance.com
API_KEY=your_api_key_here
```

Закоммитьте (без реальных секретов!):

```bash
git add .env.example
git commit -m "Add environment variables template"
git push origin main
```

### Шаг 3: Деплой на FastMCP Cloud

#### 3.1. Зайдите на FastMCP Cloud

1. Откройте: **https://fastmcp.cloud**
2. Нажмите **"Sign in with GitHub"**
3. Авторизуйте FastMCP Cloud

#### 3.2. Создайте новый проект

1. Нажмите **"New Project"** или **"Create Project"**
2. Выберите **"From GitHub Repository"**

**Опции**:
- **Use Quickstart Repo** - тестовый репозиторий от FastMCP
- **Use Your Own Repo** - ваш репозиторий ✅

Выберите **"Use Your Own Repo"**

#### 3.3. Настройте проект

Заполните параметры:

| Параметр | Значение | Описание |
|----------|----------|----------|
| **Name** | `r2r-ultra-mcp` | Имя проекта (будет в URL) |
| **GitHub Repo** | `your-username/r2r-fastmcp` | Выберите ваш репозиторий |
| **Branch** | `main` | Ветка для деплоя |
| **Entrypoint** | `mcp-server/server.py:mcp` | Путь к серверу + объект |
| **Authentication** | ✅ Enabled | Только для вашей организации |
| **Authentication** | ❌ Disabled | Публичный доступ |

**Важно для Entrypoint**:
- Если сервер в корне репозитория: `server.py:mcp`
- Если в папке `mcp-server`: `mcp-server/server.py:mcp`
- Формат: `путь/к/файлу.py:имя_объекта`

#### 3.4. Настройте переменные окружения

В настройках проекта добавьте:

1. Нажмите **"Environment Variables"** или **"Settings"**
2. Добавьте переменные:

```
R2R_BASE_URL=https://your-r2r-instance.com
API_KEY=sk-your-secret-key-here
```

**Секретность**: Эти переменные хранятся зашифрованными и недоступны в публичных логах.

#### 3.5. Запустите деплой

1. Нажмите **"Create Project"** или **"Deploy"**
2. FastMCP Cloud выполнит:
   - ✅ Клонирование репозитория
   - ✅ Установку зависимостей из `pyproject.toml`
   - ✅ Сборку сервера
   - ✅ Деплой на уникальный URL

**Процесс занимает ~1-3 минуты**

### Шаг 4: Получите URL сервера

После успешного деплоя вы увидите:

```
✅ Deployment successful!

Server URL:
https://r2r-ultra-mcp-YOUR_USERNAME.fastmcp.app/mcp
```

**Структура URL**:
- `https://` - всегда HTTPS
- `r2r-ultra-mcp` - имя проекта
- `YOUR_USERNAME` - ваш GitHub username
- `.fastmcp.app/mcp` - домен FastMCP Cloud

### Шаг 5: Подключите в Cursor

Обновите `~/.cursor/settings.json`:

```json
{
  "mcpServers": {
    "r2r-cloud": {
      "url": "https://r2r-ultra-mcp-YOUR_USERNAME.fastmcp.app/mcp",
      "transport": "sse"
    }
  }
}
```

**Без переменных окружения** - они уже установлены на сервере!

### Шаг 6: Перезапустите Cursor

```bash
# Полностью закройте Cursor
killall Cursor

# Запустите заново
open -a Cursor
```

### Шаг 7: Проверьте работу

В Cursor откройте чат и проверьте:

```
1. Проверьте доступные инструменты:
   "Какие MCP инструменты доступны?"

2. Проверьте возможности сервера:
   Используйте: get_server_capabilities()

3. Выполните поиск:
   Используйте: r2r_search_with_progress(query="test")
```

## 🔄 Автоматический редеплой

FastMCP Cloud автоматически отслеживает ваш репозиторий:

### При push в main

```bash
git add server.py
git commit -m "Update middleware configuration"
git push origin main
```

**Результат**: Автоматический редеплой (~1-2 минуты)

### При создании Pull Request

FastMCP Cloud создаст **отдельный деплой** для тестирования:

```
PR #123: https://r2r-ultra-mcp-YOUR_USERNAME-pr-123.fastmcp.app/mcp
```

Это позволяет тестировать изменения до слияния в main!

## 📊 Мониторинг и логи

### Веб-интерфейс FastMCP Cloud

1. Зайдите на https://fastmcp.cloud
2. Откройте ваш проект
3. Доступны:
   - 📈 **Metrics** - метрики запросов, латентность
   - 📝 **Logs** - логи сервера в реальном времени
   - ⚙️ **Settings** - настройки проекта
   - 🔑 **Environment** - переменные окружения

### Метрики server.py

Ваш сервер автоматически отправляет метрики:

```python
# Middleware автоматически логирует:
# ⏱️ Время выполнения каждого инструмента
# 💾 Cache hit rate
# 🚫 Rate limiting events
# ❌ Ошибки и retry attempts
```

## 🔧 Управление проектом

### Обновление переменных окружения

1. Зайдите в **Settings → Environment Variables**
2. Измените значения
3. Нажмите **"Redeploy"** для применения

### Изменение настроек

1. **Name** - можно изменить (изменит URL!)
2. **Branch** - можно переключить на другую ветку
3. **Entrypoint** - можно изменить файл/объект
4. **Authentication** - можно включить/выключить

### Удаление проекта

1. **Settings → Danger Zone**
2. **"Delete Project"**
3. Подтвердите удаление

⚠️ **Внимание**: URL станет недоступен немедленно!

## 🆚 Локальный vs Cloud

| Характеристика | Локальный (stdio) | FastMCP Cloud |
|----------------|-------------------|---------------|
| **Доступ** | Только на вашем компьютере | Из любой точки мира |
| **URL** | Нет | `https://...fastmcp.app/mcp` |
| **SSL/HTTPS** | Не нужен | Автоматически |
| **Переменные окружения** | Локальный `.env` | Веб-интерфейс |
| **Мониторинг** | Нет | Встроенная панель |
| **Автодеплой** | Нет | При push в GitHub |
| **Стоимость** | Бесплатно | **Бесплатно** (личное использование) |
| **Подходит для** | Разработка, тестирование | Продакшн, командная работа |

## 🎓 Примеры использования

### Пример 1: Разработка локально, деплой в облако

```bash
# 1. Разработка локально
fastmcp dev server.py

# 2. Тестирование
pytest tests/

# 3. Коммит и push
git add .
git commit -m "Add new feature"
git push origin main

# 4. Автоматический деплой на FastMCP Cloud ✅
```

### Пример 2: Тестирование PR перед слиянием

```bash
# 1. Создайте feature branch
git checkout -b feature/new-search-algorithm

# 2. Внесите изменения
vim server.py

# 3. Push и создайте PR
git push origin feature/new-search-algorithm

# 4. GitHub → Create Pull Request
# 5. FastMCP Cloud автоматически создаст тестовый деплой
# 6. Протестируйте на URL: https://...pr-123.fastmcp.app/mcp
# 7. Если всё ОК → Merge PR
# 8. Автоматический деплой в production ✅
```

### Пример 3: Использование из нескольких мест

```json
// ~/.cursor/settings.json на рабочем компьютере
{
  "mcpServers": {
    "r2r-cloud": {
      "url": "https://r2r-ultra-mcp-yourname.fastmcp.app/mcp",
      "transport": "sse"
    }
  }
}
```

```json
// ~/.cursor/settings.json на домашнем компьютере
{
  "mcpServers": {
    "r2r-cloud": {
      "url": "https://r2r-ultra-mcp-yourname.fastmcp.app/mcp",
      "transport": "sse"
    }
  }
}
```

**Результат**: Один сервер, доступен везде! 🌍

## ❓ FAQ

### Q: Нужно ли платить?

**A**: Нет! FastMCP Cloud бесплатен для личного использования.

### Q: Как изменить URL сервера?

**A**: Измените **Name** проекта в настройках. Внимание: старый URL перестанет работать!

### Q: Можно ли использовать приватный репозиторий?

**A**: Да! FastMCP Cloud поддерживает приватные репозитории GitHub.

### Q: Как обновить зависимости?

**A**: Измените `pyproject.toml`, закоммитьте и запуште. Автоматический редеплой установит новые зависимости.

### Q: Сколько проектов можно создать?

**A**: На бесплатном тарифе - без ограничений для личного использования.

### Q: Как работает аутентификация?

**A**: 
- **Disabled**: Сервер публичный, доступен всем
- **Enabled**: Только члены вашей организации FastMCP Cloud

### Q: Что делать если деплой не удался?

**A**: 
1. Проверьте логи в веб-интерфейсе
2. Убедитесь что `fastmcp inspect server.py:mcp` работает локально
3. Проверьте `pyproject.toml` на ошибки
4. Проверьте переменные окружения

### Q: Можно ли использовать с другими LLM клиентами?

**A**: Да! Любой клиент с поддержкой MCP может подключиться к вашему URL.

## 🔗 Полезные ссылки

- **FastMCP Cloud**: https://fastmcp.cloud
- **FastMCP Docs**: https://gofastmcp.com
- **FastMCP GitHub**: https://github.com/jlowin/fastmcp
- **Discord**: https://discord.com/invite/aGsSC3yDF4

## 🎉 Готово!

Ваш `server.py` теперь доступен из любой точки мира на FastMCP Cloud! 🚀

**Следующие шаги**:
1. ✅ Протестируйте все инструменты через Cursor
2. ✅ Настройте автоматическое тестирование для PR
3. ✅ Поделитесь URL с командой (если аутентификация включена)
4. ✅ Мониторьте метрики в веб-панели

---

**Вопросы?** Создайте issue на GitHub или спросите в Discord сообщества FastMCP!

